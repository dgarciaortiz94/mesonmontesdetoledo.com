<nav class="navbar navbar-expand-lg p-0">
    <div class="container-fluid">
        <button class="navbar-toggler text-light" type="button" data-mdb-toggle="collapse" data-mdb-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Navegación"
        >
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav d-flex align-items-center">
                {% for item in knp_menu_get('websiteMain').children %}
                    <li class="nav-item">
                        {% if (not app.request.attributes.get('_route_params')) and (path(app.request.attributes.get('_route')) == item.uri) %}
                            <div class="nav-link text-light active current">{{ item.name }}</div>
                        {% else %}
                            <a href="{{ item.uri }}" class="nav-link text-light">{{ item.name }}</a>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>