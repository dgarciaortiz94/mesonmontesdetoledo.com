{% extends 'user/base.html.twig' %}

{% block section %}
    <section>
        <div class="container">
            <div class="section-box">
                <div class="section-box__body">
                    <div class="py-2 section-box__title d-flex justify-content-between align-items-center">
                        <p class="h5"><i class="fas fa-plus"></i>&nbsp Crear usuario</p>
                        <div>
                            <a href="{{ path('app_user_index') }}"><i class="fas fa-undo-alt fa-lg"></i></a>
                        </div>
                    </div>

                    {{ include('user/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
