{% extends 'user/base.html.twig' %}

{% block section %}
    <section>
        <div class="container">
            <div class="section-box">
                <div class="section-box__body">
                    <div class="py-2 section-box__title d-flex justify-content-between align-items-center">
                        <p class="h5"><i class="fas fa-plus"></i>&nbsp Editar usuario</p>
                        <div class="d-flex">
                            <a href="{{ path('app_user_show', {'id': user.id}) }}"><i class="fas fa-eye fa-lg"></i></a>
                            <div class="mx-3">
                                {{ include('user/_delete_form.html.twig') }}
                            </div>
                            <a href="{{ path('app_user_index') }}"><i class="fas fa-undo-alt fa-lg"></i></a>
                        </div>
                    </div>

                    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
